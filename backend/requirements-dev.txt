# Dev-only deps. Installed in CI lint/audit/typecheck jobs; not baked into
# the production image.
-r requirements.txt
ruff==0.14.4
pip-audit==2.9.0
pytest-cov==6.0.0
mypy==1.13.0
# Type stubs for libraries we actually call into. pandas/numpy are intentionally
# excluded — too noisy on a never-typechecked codebase. Add later if desired.
types-requests==2.32.0.20241016

